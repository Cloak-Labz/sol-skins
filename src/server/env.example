# ============================================
# SkinVault Backend Environment Configuration
# ============================================
# Copy this file to .env and fill in your values

# ============================================
# DATABASE CONFIGURATION (PostgreSQL)
# ============================================
DB_HOST=localhost
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here
DB_DATABASE=solskins

# ============================================
# JWT AUTHENTICATION
# ============================================
# IMPORTANT: Generate a strong secret (min 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_min_32_characters_CHANGE_THIS_IN_PRODUCTION

# ============================================
# SOLANA CONFIGURATION
# ============================================
# For Devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_WS_URL=wss://api.devnet.solana.com

# For Mainnet (when ready)
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# SOLANA_WS_URL=wss://api.mainnet-beta.solana.com

# Program ID (deployed program address)
PROGRAM_ID=HkgLfcBuBwxJroAk2C874rWjp7FzkThAXBGwcoWWRPH1

# Oracle Private Key (base58 encoded)
# Generate with: solana-keygen new --outfile oracle-keypair.json
# Convert to base58: node -e "console.log(require('bs58').encode(Buffer.from(JSON.parse(require('fs').readFileSync('oracle-keypair.json')))))"
ORACLE_PRIVATE_KEY=your_oracle_private_key_base58_here